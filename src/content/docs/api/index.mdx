---
title: API Overview
template: splash
tableOfContents: false
---

<Fragment set:html={`
  <style>
    /* Force the main layout containers to full width */
    .main-frame, 
    .sl-container, 
    .sl-markdown-content {
      max-width: 100% !important;
      --sl-content-width: 100% !important;
      padding: 0 !important;
    }

    /* Remove the default side margins added by Starlight's splash template */
    main {
      padding-inline: 0 !important;
    }

    /* Optional: Hide the default page title if it's creating a gap above Scalar */
    h1 {
      display: none !important;
    }
  </style>
`} />

import { ScalarComponent } from '@scalar/astro'

<ScalarComponent configuration={{ url: '/openapi.json' }} />

